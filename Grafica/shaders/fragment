#version 330 core
in vec3 Color;
out vec4 FragColor;

uniform vec4 StreetColor;
uniform bool railway;

void main()
{
	if(railway)
	{
		FragColor = StreetColor;
	}
	else
	{
		FragColor = vec4(Color, 1.0f);
	}
}